{
    "mcpServers": {
        "browser-use": {
            "type": "stdio",
            "command": "docker",
            "args": [
                "run",
                "--rm",
                "-i",
                "-v",
                "${PWD}/screenshots:/app/screenshots",
                "technicalheist/browser-mcp-server"
            ]
        }
    }
}